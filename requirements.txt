# =========================================================================
# VCOREX Institutional Trading Bot - Core Production Dependencies
# Optimized for Python 3.14+ with Async Support
# Install: python -m pip install -r requirements.txt
# =========================================================================

# Core Asynchronous HTTP & API Integration
aiohttp>=3.10.0
aiohappyeyeballs>=2.5.0      # Required by aiohttp (pin explicitly to avoid partial installs)
aiosignal>=1.4.0
attrs>=23.0.0
frozenlist>=1.4.0
multidict>=6.0.0
propcache>=0.2.0
yarl>=1.17.0
python-telegram-bot>=21.5
httpx>=0.27.0                # Required by python-telegram-bot
httpcore>=1.0.0              # Required by httpx
websockets>=12.0

# Database Engine & Async Driver (SQLite WAL Mode)
SQLAlchemy>=2.0.30
aiosqlite>=0.20.0
greenlet>=3.1.0              # Required for SQLAlchemy Async on Windows
alembic>=1.13.0              # Database migration tool

# Configuration & Logging
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
loguru>=0.7.2

# Math & Data Analysis
pandas>=2.2.0
numpy>=2.0.0                 # Required for Python 3.14 compatibility
tenacity>=8.5.0              # Resilient retrying logic for API calls
cachetools>=5.3.0            # TTL cache for order dedup (order_handler)

# Distributed Features - Redis Streams EventBus
redis>=8.0.0

# Telemetry & System Health
psutil>=5.9.0

# File I/O
aiofiles>=23.2.1

matplotlib
mplfinance
pandas
plotly>=5.24.1
kaleido>=0.2.1
